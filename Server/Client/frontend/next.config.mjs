/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com'], // Add any other domains you might need
    },
  };
  
  export default nextConfig;
  